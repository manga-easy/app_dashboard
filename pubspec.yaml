name: dashboard_manga_easy
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sdk_manga_easy:
      git:
        url: https://github.com/lucascardo12/sdk_manga_easy.git
  google_fonts: ^2.3.1
  flutter_svg: ^1.0.3
  fl_chart: ^0.50.1
  provider: ^6.0.2
  data_table_2: ^2.2.1
  dart_appwrite: ^4.0.2
  appwrite: ^4.0.2
  awesome_notifications: ^0.6.21
  dart_appwriteold:
    path: dart_appwrite-3.0.2
  dropdown_search: ^3.0.1
  get_it: ^7.2.0
  dio: ^4.0.6

dev_dependencies:
  flutter_lints: ^1.0.4
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/images/
    - assets/icons/
  #   - images/a_dot_ham.jpeg
